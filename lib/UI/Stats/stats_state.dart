
import 'stats_initial_params.dart';

class StatsState {
  StatsState();

  factory StatsState.initial({required StatsInitialParams initialParams}) =>
      StatsState(
  );

  StatsState copyWith() =>
      StatsState(

      );
}