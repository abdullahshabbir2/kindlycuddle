import 'Home_initial_params.dart';

class HomeState {
  HomeState();

  factory HomeState.initial({required HomeInitialParams initialParams}) =>
      HomeState(
  );

  HomeState copyWith() =>
      HomeState(

      );
}